# Sids_3dx_Utils

Utilities and tools for 3DX projects.

Repo initialized by Copilot CLI. Next steps:
- Review code and run project-specific checks (lint, tests)
- Tell Copilot CLI which checks to run or allow it to autodiscover build/test commands
